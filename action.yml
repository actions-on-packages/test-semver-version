name: 'Test semver version passing'
description: 'try to define semver tag'
author: 'Aditya Srinivas'
version: '1.0.0'

inputs:
  milliseconds: # change this
    required: true
    description: 'input description here'
    default: 'default value if applicable'
 
runs:
  using: 'node16'
  main: 'dist/index.js'
